<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PointOfSaleSystem.Views.RevenuePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PointOfSaleSystem.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:lvc="using:LiveChartsCore.SkiaSharpView.WinUI"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical"
                        Padding="10">
                <StackPanel Orientation="Vertical">
                    <TextBlock Style="{StaticResource TitleTextBlockStyle}">Doanh thu theo ngày</TextBlock>
                    <DatePicker x:Name="weeklyRevenueDatePicker" 
                                DateChanged="DailyRevenueDatePicker_DateChanged" 
                                Header="Chọn ngày" 
                                Margin="5" />
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Margin="5">Tổng doanh thu của ngày: </TextBlock>
                        <TextBlock x:Name="dailyTotalRevenue" 
                                   Margin="5"></TextBlock>
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Vertical">
                    <TextBlock Style="{StaticResource TitleTextBlockStyle}">Doanh thu theo tuần</TextBlock>
                    <DatePicker DateChanged="WeeklyRevenueDatePicker_DateChanged" 
                                Header="Chọn ngày bắt đầu" 
                                Margin="5" />
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Auto">
                        <lvc:CartesianChart 
                            Series="{x:Bind WeeklyChartViewModel.Series}" 
                            XAxes="{x:Bind WeeklyXAxes}" 
                            YAxes="{x:Bind WeeklyYAxes}" 
                            Height="200"
                            Width="800"
                            LegendPosition="Right"/>
                    </ScrollViewer>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Margin="5">Tổng doanh thu của tuần:</TextBlock>
                        <TextBlock x:Name="weeklyTotalRevenue"
                                   Margin="5"></TextBlock>
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Vertical">
                    <TextBlock Style="{StaticResource TitleTextBlockStyle}">Doanh thu theo tháng</TextBlock>
                    <DatePicker DateChanged="MonthlyRevenueDatePicker_DateChanged" 
                                Header="Chọn ngày bắt đầu" 
                                Margin="5" />
                    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Auto">
                        <lvc:CartesianChart 
                            Series="{x:Bind MonthlyChartViewModel.Series}" 
                            XAxes="{x:Bind MonthlyXAxes}" 
                            YAxes="{x:Bind MonthlyYAxes}"
                            Height="200"
                            Width="800"
                            LegendPosition="Right"/>
                    </ScrollViewer>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Margin="5">Tổng doanh thu của tháng:</TextBlock>
                        <TextBlock x:Name="monthlyTotalRevenue" 
                                   Margin="5"></TextBlock>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
