<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PointOfSaleSystem.Views.AddNewTableWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PointOfSaleSystem.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="AddNewTableWindow">

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock Text="Thêm bàn mới"
               Style="{StaticResource HeaderTextBlockStyle}"
               FontWeight="Bold"
               Foreground="#ffb22c"/>
        <TextBox Header="Tên bàn" 
             x:Name="tableNameTextBox"
             PlaceholderText="Nhập bàn..."
             Margin="5"/>

        <Button Content="Thêm"
        Background="{ThemeResource SystemAccentColor}"
        Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"
        HorizontalAlignment="Center"
        Click="AddButton_Click"
        Margin="5"/>
    </StackPanel>
</Window>
